import React from "react";
import "./AuthCard.css";
interface Props {
    children: React.ReactNode;
    title: string;
}
export default function AuthCard({ children, title }: Props): React.JSX.Element;
export {};
