{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch && npm run sentry:sourcemaps",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "sentry:sourcemaps": "sentry-cli sourcemaps inject --org propel-yd --project node-nestjs ./dist && sentry-cli sourcemaps upload --org propel-yd --project node-nestjs ./dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "@sentry/cli": "^2.58.4",
    "@supabase/supabase-js": "^2.80.0",
    "jspdf-autotable": "^5.0.2",
    "openai": "^4.0.0",
    "react-markdown": "^10.1.0",
    "typescript": "^5.0.0",
    "xlsx": "^0.18.5"
  }
}
